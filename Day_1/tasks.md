# Tasks for Day 1
    0) `https://wayground.com/join?gc=41128526`
    1)  create a Repo name `Day_1_tasks`
    2)  Clone it to your local system
    3)  create a file name `tasks.md` and try to  do the following
        - all 6 headers
        Html headers (h1, h2, h3, h4, h5, h6)
        - ol, ul
        Html lists (ordered and unordered)
        - image paste
        html image tag
        - highlight, bold and italic text
        html tags for text formatting (highlight, bold, italic)
        - code block (python hello world) -> ```
        print("Hello, World!")
        ```
        - paste an image in md file
        html image tag -> <img src="path_to_image" alt="Image description">

    3) try to understand the translator code that you did today
    4) type the three git commands in an MD file and try to understand them
        - git add .
        - git commit -m "message"
        - git push origin main

![alt text](image.png)